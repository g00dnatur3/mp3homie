import assert from 'assert'

export class WalletDeposit {

  id: number = 0
  // name: string = ''
  // description: string = ''
  // county: string = ''
  // state: string = 'CA'
  // photos: string[] = []
  // recipients: string[] = []
  // defaultAmounts: string[] = []
  // goal: number = 0
  // totalRaised: number = 0
  // totalDonations: number = 0

  // static validate(campaign: Campaign) {
  //   assert(campaign.name, 'Campaign.name is missing')
  //   assert(campaign.description, 'Campaign.description is missing')
  //   assert(campaign.county, 'Campaign.county is missing')
  //   assert(campaign.state, 'Campaign.state is missing')
  //   assert(campaign.photos && campaign.photos.length, 'Campaign.photos is empty')
  //   assert(campaign.recipients && campaign.recipients.length, 'Campaign.recipients is empty')
  //   assert(campaign.defaultAmounts, 'Campaign.defaultAmounts is missing')
  //   assert(campaign.goal, 'Campaign.goal is missing')
  //   // assert(campaign.totalRaised, 'Campaign.totalRaised is missing')
  //   // assert(campaign.totalDonations, 'Campaign.totalDonations is missing')
  // }
  
}


export default WalletDeposit
